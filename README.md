# Flux Helm Operator POC

Demo of installing Flux Helm Operator and the stable Prometheus Helm Chart using kustomize.

## Dependencies

* `kubectl` + valid kubeconfig
* `kustomize`

## Demo

`./demo.sh`
